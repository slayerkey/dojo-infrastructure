# Dojo RR Tracker

Cloudflare Worker for the experimental Valorant RR tracking backend.

Deployment source of truth: this repository. Cloudflare deploys automatically from the `dojo-rr-tracker` directory on pushes to `main`.
